import { creativeExamples } from "../data"; import { Section } from "../Shared";
export default function CreativeExamples() { return <Section copy={creativeExamples} dark><div className="mt-12 grid gap-4 md:grid-cols-3">{creativeExamples.cards.map((card, index) => <article key={card.title} className={`min-h-80 rounded-3xl p-7 ${index === 1 ? "bg-[#f7bf36] text-black" : "bg-white/10"}`}><p className="text-xs font-semibold uppercase tracking-widest opacity-60">{card.label}</p><h3 className="mt-28 text-3xl tracking-tight">{card.title}</h3><p className="mt-3 text-sm leading-6 opacity-65">{card.body}</p></article>)}</div></Section> }
