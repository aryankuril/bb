import { testimonials } from "../data"; import { Section } from "../Shared";
export default function Testimonials() { return <Section copy={{ eyebrow: "What clients remember", title: "The work should make your team feel less alone in the hard decisions.", body: "A partner earns trust by being useful when the answer is not immediately obvious." }}><div className="mt-12 grid gap-4 md:grid-cols-2">{testimonials.map((item) => <figure key={item.name} className="rounded-3xl border border-black/10 p-8"><blockquote className="text-2xl leading-tight tracking-tight">“{item.quote}”</blockquote><figcaption className="mt-9 text-sm"><strong className="block">{item.name}</strong><span className="opacity-55">{item.company}</span></figcaption></figure>)}</div></Section> }
