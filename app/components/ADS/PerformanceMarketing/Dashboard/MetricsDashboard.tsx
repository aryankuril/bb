"use client";
import { useEffect, useState } from "react"; import { metrics } from "../data"; import { Section } from "../Shared";
export default function MetricsDashboard() { const [active, setActive] = useState(false); useEffect(() => { setActive(true); }, []); return <Section copy={{ eyebrow: metrics.eyebrow, title: metrics.title, body: "A practical readout for the decisions that actually alter the business." }} dark><div className="mt-12 rounded-3xl border border-white/15 p-5 md:p-8"><div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{metrics.metrics.map((metric, index) => <div key={metric.label}><p className="text-sm text-white/50">{metric.label}</p><strong className="mt-3 block text-4xl text-[#f7bf36]">{active ? metric.value : "—"}</strong><div className="mt-6 h-1 rounded bg-white/15"><div style={{ width: active ? `${55 + index * 11}%` : "0%" }} className="h-full rounded bg-[#f7bf36] transition-all duration-1000" /></div></div>)}</div></div></Section> }
