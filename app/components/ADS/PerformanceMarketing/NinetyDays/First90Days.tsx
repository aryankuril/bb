import { first90Days } from "../data"; import { Section } from "../Shared";
export default function First90Days() { return <Section copy={{ eyebrow: first90Days.eyebrow, title: first90Days.title, body: "The initial engagement is deliberately paced: fast enough to create movement, deliberate enough to build a useful learning loop." }}><div className="mt-12 grid gap-3 md:grid-cols-5">{first90Days.phases.map((phase, index) => <div key={phase.label} className="relative rounded-2xl bg-[#e9e6de] p-5"><span className="text-xs font-semibold uppercase tracking-widest text-[#d18a00]">{phase.label}</span><p className="mt-12 text-sm leading-6">{phase.body}</p>{index < 4 && <span className="absolute -right-2 top-1/2 z-10 hidden h-4 w-4 rotate-45 border-r border-t border-black/20 bg-[#f7f6f2] md:block" />}</div>)}</div></Section> }
