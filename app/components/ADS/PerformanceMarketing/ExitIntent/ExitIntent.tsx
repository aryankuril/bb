"use client";
import { useEffect, useState } from "react"; import { cta } from "../data"; import { LinkButton } from "../Shared";
export default function ExitIntent() { const [show, setShow] = useState(false); useEffect(() => { const handler = (event: MouseEvent) => { if (event.clientY < 8) setShow(true); }; document.addEventListener("mouseout", handler); return () => document.removeEventListener("mouseout", handler); }, []); if (!show) return null; return <div className="fixed inset-0 z-50 grid place-items-center bg-black/70 p-5"><div className="relative max-w-md rounded-3xl bg-[#f7bf36] p-8 text-black"><button aria-label="Close" onClick={() => setShow(false)} className="absolute right-5 top-4 text-xl">×</button><p className="text-xs font-semibold uppercase tracking-widest">Before you go</p><h3 className="mt-4 text-3xl tracking-tight">{cta.title}</h3><p className="mt-4 text-sm leading-6">{cta.body}</p><div className="mt-6"><LinkButton cta={cta.button} invert /></div></div></div> }
