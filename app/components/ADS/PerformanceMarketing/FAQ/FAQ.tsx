"use client";
import { useState } from "react"; import { faq } from "../data"; import { Section } from "../Shared";
export default function FAQ() { const [open, setOpen] = useState<number | null>(0); return <Section copy={{ eyebrow: faq.eyebrow, title: faq.title, body: "The questions that make for a more useful first conversation." }}><div className="mt-12 max-w-4xl divide-y divide-black/10 border-y border-black/10">{faq.items.map((item, index) => <div key={item.q}><button onClick={() => setOpen(open === index ? null : index)} className="flex w-full items-center justify-between gap-6 py-5 text-left text-lg font-medium"><span>{item.q}</span><span className="text-[#d18a00]">{open === index ? "−" : "+"}</span></button>{open === index && <p className="max-w-3xl pb-6 text-sm leading-6 opacity-65">{item.a}</p>}</div>)}</div></Section> }
