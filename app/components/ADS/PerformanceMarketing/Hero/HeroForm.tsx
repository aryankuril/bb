"use client";
import { useState } from "react";
import { heroContent } from "../data";
export default function HeroForm() { const [sent, setSent] = useState(false); return <form id="enquiry" onSubmit={(event) => { event.preventDefault(); setSent(true); }} className="rounded-[2rem] border border-white/20 bg-white/10 p-6 shadow-2xl backdrop-blur-xl md:p-8"><p className="title font-medium text-white">{heroContent.formTitle}</p><p className="mt-2 text-sm leading-6 text-white/55">{heroContent.formBody}</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{heroContent.fields.map((field, index) => <label key={field} className={index === heroContent.fields.length - 1 ? "sm:col-span-2" : ""}><span className="sr-only">{field}</span>{field === heroContent.fields[3] ? <select aria-label={field} className="w-full rounded-xl border border-white/15 bg-black/20 px-4 py-3 text-sm text-white outline-none"><option value="">{field}</option><option value="under-100k">Under ₹1 lakh</option><option value="100-500k">₹1–5 lakh</option><option value="500k-plus">₹5 lakh+</option></select> : <input aria-label={field} placeholder={field} required={index < 2} className="w-full rounded-xl border border-white/15 bg-black/20 px-4 py-3 text-sm text-white placeholder:text-white/40 outline-none focus:border-[#f7bf36]" />}</label>)}</div><button className="mt-4 w-full rounded-xl bg-[#f7bf36] px-4 py-3 text-sm font-semibold text-black transition hover:bg-[#ffd465]">{sent ? "We have your note" : heroContent.formButton}</button></form> }
